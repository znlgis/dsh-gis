{
  "name": "dsh-gis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GIS capability for DeepSeek Harness: formats, tools, map rendering, OGC services",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "echo '(lint placeholder)'"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsdown": "^0.22.2",
    "lightningcss": "^1.32.0",
    "vitest": "^3.0.0",
    "react": "^18.2.0",
    "@types/react": "~18.3.1",
    "@types/node": "^24.0.0"
  }
}

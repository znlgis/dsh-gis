/**
 * dsh-ui-gis host half.
 *
 * Deliberately empty: this package exists so the browser half rides a stable
 * Loader row whose specifier is the bare package name (design 3.3).
 */

/** Provides no host-side behavior. */
export function apply(): void {}

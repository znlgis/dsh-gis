feat: T2.8 — GIS document previews

Two registrations, both needed (metadata without a body shows an empty tab)
- text family (loading: 'text-pages'): .geojson, .topojson, .ndjson, .geojsonl,
  .jsonl, .wkt, .kml, .gpx
- binary family (loading: 'bytes-complete'): .fgb, .tif/.tiff/.cog, .pmtiles,
  .shp, .gpkg
- both declare priority: 'extension', which is what makes the host rank them
  above its built-in text preview
- .json is deliberately NOT claimed: a config file should open as text, not as
  a failed map

Bodies (lazy, behind eager shims, reusing the card's readers)
- GeoJSON is drawn by the SHARED map component -- one implementation, two
  surfaces -- and a file that cannot be drawn still shows its text plus the
  reason
- FlatGeobuf is decoded into a GeoJSON source
- GeoTIFF/COG is decoded into a canvas
- formats we claim but cannot draw say so in words; above 8 MB of text or
  48 MB of bytes the preview refuses and points at the card, which streams

Verified
- 194 node tests + 1 skipped (10 new: definition shape, parsers, refusals)
- 13 web-lane tests (6 new: real components with real fixtures and a fake
  MapLibre loader, including a real .fgb drawn on the map)

Also fixed: the flattener's "skip dispatcher chunks" rule from the T2.5 round
was too strict. The shared map component is a dispatcher AND a dependency of
two preview bodies, so the bundle again carried a forbidden synchronous chunk
require. The only skip condition now is "inlining would graft the requirer into
itself" (contract #39).

Honest boundary: the registrations and the click-through in the real app are
NOT verified -- that needs an isolated workspace the check may write into, and
the harness does not redirect the workspace yet. Recorded for T2.10.

# dsh-gis 实施计划

> 本计划把 [方案设计.md](./方案设计.md)（v2，16 章 + 8 附录）转成可执行工单。**设计回答「是什么、为什么」，本计划回答「谁、何时、怎么算做完」。**
>
> **估算口径**：1 人周 = 5 人日；面向「熟悉 TypeScript 但**不熟悉 DSH 内部**」的工程师，**含自测**，不含评审等待与外部依赖等待。
>
> ⚠️ 本计划是**自下而上**重估的：逐任务拆完后合计 **≈85.5 人日 ≈ 17.1 人周**，比设计文档路线图的 ≈14.5 人周高约 18%。差值与取舍见第十节——这是本计划相对设计文档的第一个实质修正。

---

## 一、总览

| 里程碑 | 内容 | 人日 | 前置 | 可并行于 |
|---|---|---|---|---|
| **M0** | 脚手架 + **可行性门（真实客户端构建）** | 5.5 | — | M1a |
| **M1a** | 懂 + 用（**零客户端代码**） | 15 | — | M0 |
| **M1b** | 能配 + 能发现（GDAL/QGIS 探测 + 设置卡片） | 13 | M0, M1a | M2 数据面 |
| **M2** | 浏览（共享地图组件 + 2 个宿主面） | 16 | M1a, M1b | M3 |
| **M3** | PostGIS + GDB 只读 | 11 | M1b | M2 |
| **M4** | OGC 地图服务（**可独立取消**） | 17 | M1b, M2 | M5 |
| **M5** | 发布与加固 | 8 | M2 | M4 |

> ### 📍 实际进度（回写于 M1b 完成后）
>
> | 里程碑 | 计划 | 状态 | 实际 |
> |---|---|---|---|
> | M0 | 5.5 | ✅ **通过** | T0.1/.2/.4/.5/.6/.7/.8 全部达成；**T0.3 因本机无模型凭据未做**（插件激活与工具注册已证，仅缺一次真实调用）。T0.4 实际 **0.5**（低于估算 1.5） |
> | M1a | 15 | ✅ **退出判据全部达成** | T1a.1/.3/.4/.5/.6/.7/.8/.9/.11/.12/.13 完成；**T1a.2（持久化）、T1a.10（缓存配额）未做**，影响是「重启后数据集失效」与「缓存无上界」，属工程质量 |
> | M1b | 13 | ✅ **主体完成** | 执行接缝 / 探测 / 设置 schema / 运行时装配 / **设置卡片端到端（含落盘）** / **GDAL 映射（真实二进制验证）** 全部完成。`.gdb` 样本由 `ogr2ogr` 生成 |
> | M2–M5 | 52 | ⏳ 未开始 | — |
>
> **累计**：8 个包构建通过，测试 **47 通过 + 1 跳过**；真实实例中 **6 个 Loader 行全部运行中**。

### 完成过程中对计划的修正（**重要，后续排期请以此为准**）

| # | 计划原文 | 实测结论 | 影响 |
|---|---|---|---|
| 1 | T1a.3/T1a.5 用 `@terraformer/wkt` 与 `shpjs` | **两者都弃用，改为自研**。`shpjs@6.2.0` 不带类型、不导出 `parseShp`/`parseDbf`，**DBF 解码不接受编码参数**——而「GBK 属性正确」是硬判据，用管不了编码的库去满足它是自相矛盾 | 选型表需改写；换来编码与 WKT 语义完全可控 |
| 2 | T1a.6 依赖 `geotiff.js` / `turf` | 未引入。M1a 的范围不需要它们 | 依赖面比计划小 |
| 3 | T1a.12 出图 | 用 **Node 内置 `zlib` 手写 PNG + 手写光栅化**，零新依赖 | T1a.12 成本低于估算 |
| 4 | 新增（计划中没有） | **`gis_doctor` 工具**：让探测结果可被模型使用 | M1b 必要补充 |
| 5 | 新增（计划中没有） | **`docs/运行时契约.md`**：17 条契约陷阱 | 实现新组件前必读 |
| 6 | 「必须起真实实例才能验证」 | **不完全成立**。M1b 的 GDAL 链路靠把 `runtime.run` 抽成接口，在单元测试里驱动了**真实二进制**，不需要凭据 | 后续里程碑应优先寻找这种解耦点，可显著降低验证成本 |

### 对 M2 及之后的建议

1. **M2 开工前先读 `docs/运行时契约.md`。** 它记录的 17 条陷阱里，客户端半占 6 条，而 M2 正是客户端工作最密集的阶段。
2. **把「真实实例验证」写进每个里程碑的退出判据**，但**同时要求**给出「能否用结构化解耦在单测中覆盖」的论证——两者结合才是成本最优。
3. **T1a.2 建议并入 M2 之前**：数据集不能跨进程存活，在浏览层会立刻变成可见缺陷。

### T1a.2 的起步资料（已侦察，未实施）

`ctx.storageDomain` 的 API 已定位，下一次可直接从这里开始：

```
declare module '@deepseek-ai/cordis' { interface Context { storageDomain: DomainFacility } }
  // 声明于 packages/storage/storage-domain/src/index.ts:35-39

class DomainFacility {
  async open<S extends DomainSpec>(spec: S): Promise<Domain<S>>
  // 生命周期：调用方持有返回的 handle，并负责 Domain.close()（通常作为自己的 ctx.effect 销毁器）
}

defineDomain<S extends DomainSpec>(spec: S): S            // spec.ts:107
interface Domain<S> {                                     // domain.ts:97
  table(name: string): KvTable<string, unknown>           // domain.ts:217
  close(): Promise<void>                                  // domain.ts:118
}
```

**两个必须注意的点**：

1. **`open()` 是异步的，且一个 domain 名只能开一次**（`already-open` 会抛错）。要在 `ctx.effect` 里打开并关闭。
2. **`gis-core` 目前用类形式，注入必须写成 `static inject`**（见运行时契约第 1 条）。若把 `storageDomain` 加进注入列表，**`gis-core` 就会等待该服务**——profile 若未挂载 storage，`gis-core` 将永远不激活。**建议做成可选依赖**：不加进 inject，改用 `ctx.get('storageDomain')` 之类的可选访问，缺省时退回内存注册表。

**已有的基础**：id 派生（`deriveDatasetId`，内容派生、稳定）与 `DATASET_NOT_FOUND` 结构化 404 语义都已实现并有测试；缺的只是把注册表落到 storage 并跨进程恢复。

| | **合计** | **85.5** | | |

**工期换算**：2 人 ≈ **10 周**（含 15% 缓冲）；1 人 ≈ **18 周**；3 人不缩短关键路径（见第五节），只加快 M3/M4 的并行面。

---

## 二、两条并行轨道

设计把 1a 与客户端风险解耦，正是为了让两条轨道能同时开工：

| | 轨道 A —— 客户端 / 构建 | 轨道 B —— 宿主 / 数据 |
|---|---|---|
| 技能面 | tsdown、浏览器 bundle、React、插槽、CSS | Node、GDAL/QGIS、格式解析、SQL、HTTP 客户端 |
| 顺序 | M0 → M1b（设置卡片）→ M2（两个宿主面）→ M5 | M1a → M1b（Provider/探测）→ M3 → M4 |
| 关键路径 | **是**（M0 是硬门） | 否（但 M1a 是 M2 的前置） |

> **单人团队的做法**：按 M0 → M1a → M1b → M2 → M3 →（M4 可选）→ M5 串行，别试图交错。

---

## 三、工作分解（WBS）

**完成判定（DoD）统一要求**（不逐条重复）：产出物已提交；`pnpm test` 绿；新增公开行为有测试；触及注册表的改动有 **HMR 安全测试**（dispose 后无残留，`docs/testing.md:9`）；新增文案走 `t()`；`pnpm lint` 绿。

### M0 —— 脚手架与可行性门（5.5 人日）

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T0.1 | 仓库脚手架 | `pnpm-workspace.yaml`、`tsconfig.base.json`、`tsdown.config.ts`、`vitest.config.ts`、lint | — | 0.5 | 空包上 `build` 与 `test` 均绿 |
| T0.2 | 根 bundle 包 | `@znlgis/dsh-gis` + `package.json.dsh.bundle.patch` + `cordis.patch.yml` | T0.1 | 0.5 | `dsh --profile gis --dump-config` 出现本项目层 |
| T0.3 | 宿主冒烟 | 一个探测工具 | T0.2 | 0.5 | GUI 内调用成功并回传结果 |
| T0.4 | **复刻 `clientBundle` preset** | 本地 `tsdown.client.ts`（替换 **4 处**仓库内 import） | T0.1 | **0.5**（M0 实测：值导入闭包只有 4 个文件，其余是类型导入会被擦除） | 产出 `lib/client.js`，`window.__ModuleLoader__.load({id,factory})` 包装与平台 external 正确 |
| T0.5 | **真实 bundle 验证** | 两组件 + 两 chunk（其中一个 `import()` 懒加载）+ CSS Modules | T0.4 | 1.0 | 两组件都渲染、样式生效、**懒 chunk 不在首屏请求里** |
| T0.6 | HMR 验证 | — | T0.5 | 0.25 | 改 `src/client/**` → 免刷新重载，**旧 `<style data-plugin>` 被清掉** |
| T0.7 | 设置链路验证 | `plugins.bundle.config` 最小卡片 | T0.2 | 0.75 | 改 `.volatile()` 字段 → 写入 profile 的 Cordis patch；重启后仍在 |
| T0.8 | 执行链路验证 | `sandbox.confine` → `subprocess.spawn` 原型 | T0.2 | 0.25 | 跑通 `ogr2ogr --version`；`env` 注入生效；`undefined` **墓碑**确实删掉变量 |
| T0.9 | **决策门 G0** | 评审记录（模板见第四节） | T0.5,T0.7,T0.8 | 0.25 | 三项全过 → 放行；否则按第四节分支 |

> **T0.4/T0.5 是本计划的关键路径起点**，也是最可能超时的地方。若 T0.4 超过 3 人日仍未产出可加载的 `lib/client.js`，立即触发 G0 的降级分支，不要继续投入。

### M1a —— 懂 + 用（零客户端代码，15 人日）

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T1a.1 | `gis-core` 骨架 | `ctx.gis` 门面 + Provider 接口 + 附录 C 全部类型 | T0.2 | 1.0 | 类型可被外部包 import；Provider 缺失时门面给出明确错误 |
| T1a.2 | 数据集登记 | `storageDomain` 定义 + id 派生 + 404 语义（设计 6.6） | T1a.1 | 1.5 | 同一数据重复扫描得同一 id；源变更得新 id；未知 id 返回结构化 404 |
| T1a.3 | WKT / EWKT / WKB | 解析与序列化 + 轴序归一 | T1a.1 | 1.5 | 往返不丢精度；EWKT 的 SRID 被保留；轴序统一 lon,lat |
| T1a.4 | GeoJSON / NDJSON | 流式读取 + RFC 7946 违例检测 | T1a.1 | 1.0 | 投影坐标**报错**；NDJSON 不一次性 parse |
| T1a.5 | SHP 文件族 | `.prj` 检测、`.cpg` / 显式 `encoding` | T1a.1 | 1.5 | 缺 `.prj` → `CRS_UNKNOWN`；GBK 属性中文正确；文件族整体登记 |
| T1a.6 | `gis-purejs` Provider | shpjs / geotiff.js / proj4 / turf 接线 | T1a.3-5 | 1.0 | 无 GDAL 时仍能完成 inspect/query/render |
| T1a.7 | `gis_inspect` | 多图层、CRS、编码、capabilities、issues | T1a.6 | 1.5 | 返回真实要素数与范围；issues 用附录 D 的码 |
| T1a.8 | `gis_query` | where / bbox / limit / offset / `geometry=wkt` | T1a.6 | 1.0 | 分页稳定；必回 `truncated`；几何默认 WKT |
| T1a.9 | `gis_crs` | describe / transform | T1a.3 | 0.5 | 点换算有往返精度断言 |
| T1a.10 | 派生缓存 | 配额 + LRU | T1a.2 | 1.0 | 超配额按 LRU 淘汰；缓存键含 mtime/size |
| T1a.11 | `prompt-gis` | 系统提示词分节 + **2 个**技能 | T1a.7 | 0.5 | 分节 order 为有限值；技能 `content` 字段正确 |
| T1a.12 | `gis_render`（服务端） | PNG 出图 → `present` | T1a.7 | 1.0 | 交付物出现在 `present` 卡片并可打开 |
| T1a.13 | fixture + 单元测试 | 见第七节 | T1a.3-5 | 1.5 | 三类静默错误各有断言（缺 CRS、GBK、越界坐标） |
| T1a.14 | **M1a 退出验证** | 验证记录 | 全部 | 0.5 | 见第六节 M1a |

### M1b —— 能配 + 能发现（13 人日）

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T1b.1 | 执行封装 | `sandbox.confine` → `subprocess.spawn`，含超时 / 输出上限 / `ctx.spill` | T0.8 | 1.0 | 超时被掐断；超大输出落 spill 不进上下文 |
| T1b.2 | 环境注入 | `extraEnv` / `unsetEnv` → spawn `env` | T1b.1 | 1.0 | 墓碑删变量有断言；凭证形状的键不被日志记录 |
| T1b.3 | `gis-gdal` Provider | 发现链 + `--version` / `ogrinfo --formats` 探测 | T1b.1 | 1.5 | 三级发现顺序正确；探测失败给出可执行诊断 |
| T1b.4 | `gis-qgis` Provider | `qgis_process` 发现 + 版本 | T1b.3 | 0.5 | 未安装时不报错，只在 capabilities 里标不可用 |
| T1b.5 | `gis_convert` | 转换 / 重投影 / 裁剪 / 编码 / 选层 | T1b.3 | 2.0 | 输出可被 `gis_inspect` 读回且 CRS 正确 |
| T1b.6 | `gis_capabilities` | 能力自述工具 | T1b.3,T1b.4 | 0.5 | 一份返回讲清 GDAL/QGIS/GDB 驱动/连接/格式读写能力 |
| T1b.7 | `gis_env` | 环境回显工具 | T1b.2 | 0.5 | 返回解析后的 env 与可 source 的前缀 |
| T1b.8 | 设置卡片（客户端） | `plugins.bundle.config` + `SettingsForm` + 检测按钮 | T0.7 | 2.0 | 字段保存后落 profile patch；「检测」显示版本与 `GDAL_DATA`/`PROJ_LIB` 实际值 |
| T1b.9 | 检测的后端通道 | 触发探测并回传的 exact-path 路由 / Remote | T1b.8,T1b.3 | 1.0 | 路由用 exact path + query（设计 3.5） |
| T1b.10 | 错误码贯通 | 附录 D 全部码接入工具输出 | T1b.5 | 1.0 | 每个码至少一条测试触发 |
| T1b.11 | **M1b 退出验证** | 干净机器验证记录 | 全部 | 1.0 | 见第六节 M1b |

### M2 —— 浏览（16 人日）

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T2.1 | 共享地图组件骨架 | MapLibre **懒加载**接入 + 生命周期 | M0 | 2.0 | 首屏不含 MapLibre chunk；卸载无泄漏 |
| T2.2 | 图层模型与样式 | `LayerSpec` 归一、空 / 错 / 载态、`--dsw-alias-*` | T2.1 | 1.5 | 三种图层来源（本地 / 连接 / 服务）走同一组件 |
| T2.3 | Range 路由 | `/api/gis/blob?id=`（206 / `Accept-Ranges` / `Content-Range`） | T1a.2 | 1.5 | 单段与多段 Range 请求有测试；越权 id → 404 |
| T2.4 | COG 生成与直读 | 后台任务（进度 / 取消 / 配额预检）+ geotiff.js | T2.3,T1a.10 | 2.5 | 首次转换有进度且可取消；配额不足时**先拒绝再失败** |
| T2.5 | PMTiles / FlatGeobuf | 取数路径 | T2.3 | 1.0 | 两者都能按 Range 流式取用 |
| T2.6 | 地图卡片 | `tool.call.toolview[gis_render]` + `presentationMeta` 契约 | T2.2 | 2.0 | 卡片为**纯函数**：回放与实时渲染结果一致 |
| T2.7 | 回放与降级 | `isError`、`call===null`、id 404 → 「数据已不可用」 | T2.6 | 1.0 | 三种降级各有 e2e 断言，**均不抛异常** |
| T2.8 | 文档预览 | `ctx.documentPreviews.register` + 预览体 | T2.2 | 2.0 | `.geojson/.tif` 可预览；`priority:'extension'` 生效 |
| T2.9 | 地图状态 projection | `ctx.sessionProjections.register`（设计 6.5） | T2.6 | 1.0 | 回放后图层状态与实时一致 |
| T2.10 | Web e2e | 卡片渲染 / 回放 / 降级 / 懒 chunk | T2.6-9 | 1.5 | 纳入 `pnpm test:web` |
| T2.11 | **M2 退出验证** | 2 GB GeoTIFF 首次 / 再次 | 全部 | 1.0 | 见第六节 M2 |

### M3 —— PostGIS 与 GDB（11 人日；**GDB 在 v1 只读——这是产品选择，不是技术限制**）

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T3.1 | 连接剖面与凭证 | `pg` 连接 + `ctx.credentials` 引用 | M1b | 1.0 | 配置里无明文密码；`describe()` 只回「已配置」 |
| T3.2 | 目录与元数据 | `geometry_columns` + `ST_EstimatedExtent` + `reltuples` | T3.1 | 1.5 | **不执行 `count(*)`**；无统计信息时优雅降级 |
| T3.3 | 分页查询与超时 | `LIMIT/OFFSET` + `statement_timeout` + 默认只读 | T3.2 | 1.0 | 慢查询返回 `SQL_TIMEOUT`；写操作被拒 |
| T3.4 | 服务端 MVT | `ST_AsMVTGeom` + `ST_AsMVT` | T3.3 | 2.0 | 瓦片由数据库生成，**不依赖 GDAL** |
| T3.5 | GDB 读 | `OpenFileGDB` 探测 + 版本门槛（**读 ≥ GDAL 1.11 / 写 ≥ 3.6**）+ 诊断文案 | T1b.3 | 1.5 | 版本不足时断言**文案键**而非整句；**不实现写**（附录 E） |
| T3.6 | 多图层贯通 | `layer` 参数贯穿 inspect / query / render | T3.5 | 1.0 | 一个 `.gdb` 的全部要素类可逐层浏览 |
| T3.7 | 安全测试 | 注入 / 白名单 / 超时 / 只读 / 不回显 | T3.3 | 1.5 | 标识符未白名单即拒绝；参数化覆盖所有用户输入 |
| T3.8 | **M3 退出验证** | 验证记录 | 全部 | 1.5 | 见第六节 M3 |

### M4 —— OGC 地图服务（17 人日，**可独立取消**）

> 取消本里程碑不影响 M0–M3、M5 的任何验收。建议在 M2 退出后再决定是否投入。

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T4.1 | Capabilities 解析 | 流式（saxes）+ 归一 + TTL 缓存 | M1b | 2.0 | 30 MB 文档可解析；缓存命中不重取 |
| T4.2 | 代理层 | 超时 / 重定向上限 / 响应上限 / 并发限速 / 重试退避 / 凭证 / 缓存键 | T4.1 | 3.0 | 黑洞超时、重定向环、超大响应、并发上限**各一条测试** |
| T4.3 | SSRF 防护 | 逐跳复检 + 全局单播 + 解析结果直连 | T4.2 | 2.0 | 十进制 IPv4、IPv6、302 跳私网**各一条拒绝测试** |
| T4.4 | WMS → 瓦片适配 | z/x/y → GetMap + 缓存 | T4.2 | 1.5 | 1.1.1 与 1.3.0 都能出图 |
| T4.5 | WMTS 矩阵映射 | `ScaleDenominator` / `TopLeftCorner` / Y 翻转 / 非 256 | T4.2 | 2.5 | 四种矩阵集 fixture 全部映射正确 |
| T4.6 | WFS 取数 | 分页 + GML 三代包装（**2.0 `wfs:member` / 1.1 `gml:featureMember` / 1.0 `gml:featureMembers`**） | T4.2 | 2.0 | **标准 WFS 2.0 必须解析出要素而不是 0 个**；`numberMatched="unknown"` 不当数字；不支持的构造**报错而非丢几何** |
| T4.7 | 轴序归一 | 统一 `CRS:84` + 记录实际 URL | T4.4-6 | 1.5 | 已知坐标的 BBOX 顺序有断言 |
| T4.8 | 服务工具 | `gis_service_add` / `_info` / `_fetch` | T4.3 | 1.0 | 返回值含实际请求 URL 与署名 |
| T4.9 | 离线测试基建 | fixture + 本地假服务 | T4.2 | 1.5 | **CI 不出网**即可跑完全部 OGC 测试 |
| T4.10 | **M4 退出验证** | 验证记录 | 全部 | 0.5 | 见第六节 M4 |

### M5 —— 发布与加固（8 人日）

| ID | 任务 | 产出物 | 依赖 | 人日 | 完成判定 |
|---|---|---|---|---|---|
| T5.1 | 打包与产物清单 | `files` / `exports` + `pnpm pack` 核对 | M2 | 1.0 | 包内含 `lib/client.js` 与 patch；无多余文件 |
| T5.2 | 第三方声明 | `THIRD_PARTY_NOTICES` 生成 | T5.1 | 1.0 | 内联进 `lib/client.js` 的库全部列出 |
| T5.3 | CI | Windows + Linux 矩阵 | T5.1 | 1.5 | 两平台绿；含 HMR 安全测试 |
| T5.4 | 干净安装验证 | 空 profile 安装记录 | T5.3 | 1.0 | `dsh plugin add <tarball>` 后可用 |
| T5.5 | i18n 补齐 | 文案与 `locale/en.json` | T2 | 1.0 | 无硬编码中文进模型上下文 |
| T5.6 | 版本与迁移 | `engines.dsh` + `schemaVersion` | T5.1 | 0.5 | 旧数据可被新代码识别或明确拒绝 |
| T5.7 | README | 三条安装路径 + `allowBuilds` 告知 | T5.4 | 0.5 | 照 README 能在干净机器装通 |
| T5.8 | 最终验收 | 对照设计第十五章逐行勾选 | 全部 | 1.5 | 全部可勾选或写明例外 |

---

## 四、决策门

### G0 —— M0 结束时（**唯一硬门**）

| 检查项 | 通过标准 | 不通过怎么办 |
|---|---|---|
| T0.5 真实 bundle | 两组件渲染、样式生效、懒 chunk 按需到达 | **地图卡片降级**为服务端出图 + `present`；M2 缩为「文档预览 + 状态 projection」 |
| T0.6 HMR | 免刷新重载且旧样式被清掉 | 允许降级为「改客户端需刷新页面」，但需记入 README |
| T0.7 设置链路 | `.volatile()` 字段落 profile patch 且重启保留 | **不可降级**：这是需求 7 的唯一载体，做不到就必须**显式改需求** |
| T0.8 执行链路 | `confine`→`spawn` 跑通且墓碑生效 | 不可降级（否则「不配系统环境变量也能用」的承诺不成立） |

**G0 输出**：一页评审记录（三项结论 + 是否进入降级分支 + 受影响里程碑）。

### 后续检查点

| 门 | 时点 | 判据 |
|---|---|---|
| G1 | M1a 结束 | 三类静默错误（缺 CRS / GBK / 越界坐标）均有断言；一次真实出图成功 |
| G2 | M1b 结束 | **干净机器不动系统环境变量**，设置页点「检测」即通过 |
| G3 | M2 结束 | 2 GB GeoTIFF 首次有进度、再次 ≤1.5 s；回放可复现；id 失效优雅降级 |
| G4 | M3 结束 | GDB 全图层可见 + 版本不足给可执行诊断；慢查询被掐断 |
| G5 | M4 结束 | CI 离线跑通全部 OGC 契约测试 |
| G6 | M5 结束 | 干净机器照 README 安装成功；两平台 CI 绿 |

---

## 五、关键路径与排期

**关键路径**：`T0.4 → T0.5 → T0.7 → T1b.8 → T2.1 → T2.6 → T2.11 → M5`

即「preset 复刻 → 真实 bundle → 设置卡片 → 地图组件 → 卡片 → 发布」。**M1a 与 M3/M4 都不在关键路径上**，可以吸收人力。

### 2 人排期（约 10 周，含缓冲）

| 周 | 轨道 A | 轨道 B |
|---|---|---|
| 1 | T0.1–T0.4（preset 复刻） | T1a.1–T1a.3（core + WKT） |
| 2 | T0.5–T0.9（bundle + 三个验证 + **G0**） | T1a.4–T1a.6（GeoJSON / SHP / purejs） |
| 3 | T1b.8–T1b.9（设置卡片 + 检测通道） | T1a.7–T1a.10（inspect / query / crs / cache） |
| 4 | T1b.1–T1b.2（执行封装 + 环境注入） | T1a.11–T1a.14（prompt / render / fixture / **G1**） |
| 5 | T2.1–T2.2（地图组件 + 图层模型） | T1b.3–T1b.7（GDAL/QGIS + 能力发现） |
| 6 | T2.3–T2.5（Range + COG + PMTiles） | T1b.5/T1b.10–11（convert + 错误码 + **G2**） |
| 7 | T2.6–T2.7（卡片 + 降级） | T3.1–T3.4（PostGIS） |
| 8 | T2.8–T2.11（预览 + 状态 + e2e + **G3**） | T3.5–T3.8（GDB + 安全 + **G4**） |
| 9 | T5.1–T5.3（打包 + 声明 + CI） | T4.1–T4.3（Capabilities + 代理 + SSRF） |
| 10 | T5.4–T5.8（发布 + **G6**），缓冲 | T4.4–T4.10（WMS/WMTS/WFS + **G5**） |

> M4 若取消，轨道 B 在第 9–10 周并入 T5，整体可提前约 1 周。

---

## 六、各里程碑退出验证

| 里程碑 | 验证方式 | 判据 |
|---|---|---|
| **M0** | 手工 + GUI | G0 三项 |
| **M1a** | 单测 + 一次真实会话 | fixture 三类静默错误全有断言；模型问「这是什么数据、画出来看看」→ 交付 PNG |
| **M1b** | **干净机器**手工 | 不动系统环境变量；设置页「检测」显示版本与 `GDAL_DATA`/`PROJ_LIB` 实际值；`unsetEnv` 删掉污染变量后坐标系恢复 |
| **M2** | e2e + 大文件 | 首次转换有进度可取消；再次 ≤1.5 s；回放一致；`isError`/`call===null`/id 404 三种降级不抛异常；首屏无 MapLibre chunk |
| **M3** | 集成 + 安全 | `.gdb` 全图层可见；版本不足给文案键（读 ≥ 1.11 / 写 ≥ 3.6）；慢查询 `SQL_TIMEOUT`；注入测试全绿；`ST_EstimatedExtent` 为 NULL 时不崩且明确降级 |
| **M4** | **离线** fixture + 假服务 | 轴序（1.1.1/1.3.0）、WMTS（KVP/RESTful、Y 翻转、非 256）、GML 报错不丢几何；SSRF 三类拒绝；CI 不出网 |
| **M5** | 干净机器 | 照 README 装通；`THIRD_PARTY_NOTICES` 在包内；两平台 CI 绿 |

---

## 七、fixture 清单（必须提前准备，否则退出条件无法执行）

| # | fixture | 服务于 | 要点 |
|---|---|---|---|
| F1 | 一份正常 GeoJSON | M1a | 含 `bbox` 与 `id` |
| F2 | 一份**投影坐标**的 GeoJSON | M1a | 必须触发报错而非静默出图 |
| F3 | 一份**缺 `.prj`** 的 SHP | M1a | 触发 `CRS_UNKNOWN` |
| F4 | 一份 **GBK 编码**属性表的 SHP | M1a | 显式 `encoding` 后中文正确 |
| F5 | 一份 NDJSON（≥10 万行） | M1a | 验证流式读取与截断 |
| F6 | 一份多要素类 `.gdb`（**v10.x**） | M3 | 图层枚举；同时备一份低版本 GDAL 环境用于断言升级诊断 |
| F7 | 一个 PostGIS 容器 + 建表脚本 | M3 | 含无主键表与 `geography` 列各一 |
| F8 | 2 GB 级 GeoTIFF | M2 | 首次转换耗时不可忽略，用于验证进度 |
| F9 | WMTS Capabilities ×4 | M4 | 覆盖 KVP/RESTful、Y 翻转、非 256 |
| F10 | WMS 1.1.1 与 1.3.0 Capabilities | M4 | 轴序对照 |
| F11 | WFS 2.0 Capabilities + GML（`wfs:member`，**必须含一个标准 2.0 响应**）+ 一个 WFS 1.1 响应 + 一个含不支持构造的响应 | M4 | 三代包装都要能解析；标准 2.0 响应**不得返回 0 要素** |
| F12 | 本地假 OGC 服务 | M4 | 可注入超时 / 重定向环 / 超大响应 |

---

## 八、风险监控点

| 风险（设计第十四章） | 在哪个门观察 | 触发信号 | 响应 |
|---|---|---|---|
| 1 客户端构建复刻失败 | **G0** | T0.4 超 3 人日无产物 | 走降级分支；M2 缩小 |
| 2 内核 alpha 漂移 | 每次升级 | `--dump-config` 或加载失败 | 锁版本；先跑真实组合测试 |
| 3 无 GDAL 环境 | G2 / G4 | 探测报不可用 | 纯 JS 兜底；GDB 给可执行诊断 |
| 4 静默错误（CRS/编码/轴序/GML 包装） | G1 / G5 | fixture 断言失败 | 阻断发布，不得降级为警告 |
| 5 安全（越权/执行/注入/SSRF） | G4 / G5 | 安全测试未覆盖新路径 | 阻断发布 |
| 6 大数据压垮 | G3 | 首屏超预算 / 上下文被烧穿 | 收紧 `limit` 与 simplify |
| 7 缓存撑爆磁盘 | G3 | 配额告警 | LRU + 清空入口 |
| 9 远端不可控 | G5 | 离线测试无法覆盖 | 补假服务用例 |
| 10 工作量 | 每周 | 实际人日 > 计划 20% | 先砍 M4，再砍 T2.8 文档预览 |

---

## 九、交付物清单

**代码**：`dsh-gis` monorepo（7 个包）——`gis-core`、`gis-formats`、`gis-gdal`、`gis-purejs`、`gis-postgis`、`tool-gis`、`prompt-gis`、`ui-gis`（客户端半）。

**文档**：本计划、`方案设计.md`、`审阅意见-方案设计.md`、`README.md`（含三条安装路径）、`THIRD_PARTY_NOTICES`。

**测试资产**：F1–F12 fixture、离线 OGC 假服务、Web e2e、HMR 安全测试。

**运行时产物**：npm 包 / tarball、`cordis.patch.yml`、`lib/client.js`（含 `THIRD_PARTY_NOTICES` 覆盖的第三方代码）。

---

## 十、与设计文档路线图的差异（自下而上重估结果）

| 里程碑 | 设计路线图 | 本计划 | 差异原因 |
|---|---|---|---|
| M0 + M1a | 1 + 2 = 3 人周 | 4.1 人周 | 格式层（WKT/SHP/GeoJSON）与 fixture 的真实成本高于按人周粗估 |
| M1b | 2 人周 | 2.6 人周 | 设置卡片是**客户端工程**，含后端检测通道 |
| M2 | 3 人周 | 3.2 人周 | COG 后台任务（进度/取消/配额）此前被低估 |
| M3 | 2 人周 | 2.2 人周 | 安全测试独立计 |
| M4 | 3 人周 | 3.4 人周 | 代理层与 SSRF 是两个独立工作包 |
| M5 | 1.5 人周 | 1.6 人周 | 基本一致 |
| **合计** | **≈14.5 人周** | **≈17.1 人周** | **+18%** |

**结论**：设计的路线图乐观约 18%。三个选项：

1. **接受 17 人周**（推荐）——数字来自逐任务拆解，比粗估可信；
2. **砍掉 M4**（−3.4 人周）→ 13.7 人周，且不影响任何其他需求；
3. **砍掉 T2.8 文档预览 + T3.4 服务端 MVT**（−4 人日）→ 再省约 1 人周。

---

## 十一、立即可做的五件事（第一周）

1. **建仓库骨架并跑通 `--dump-config`**（T0.1–T0.2）——半天内可验证插件机制理解是否正确；
2. **复刻 `tsdown.client.ts` 并产出第一个可加载的 `lib/client.js`**（T0.4）——**全项目最大的不确定性，应该第一个碰**；
3. **准备 F1–F5 fixture**（可并行，不占工程师时间，但阻塞 M1a 的退出条件）；
4. **验证 `ctx.sandbox.confine` → `ctx.subprocess.spawn` 跑通 `ogr2ogr --version`**（T0.8）——半小时，但它决定「不配系统变量也能用」这个卖点成不成立；
5. **写 G0 评审模板**（三项检查 + 降级分支）——让第一周结束时有明确结论，而不是模糊地「差不多了」。

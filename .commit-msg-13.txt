chore: follow the kernel to 0.1.7-rc.2 (contract #34, for real this time)

check:real started failing with "startup failed: 1 required plugin did not
activate" in three separate boots. Nothing in the GIS code was involved: the DSH
kernel had moved from 0.1.7-rc.1 to 0.1.7-rc.2, and a peer-pinned plugin is
SKIPPED SILENTLY -- the log line is "skipping profile bundle @znlgis/dsh-gis:
incompatible with dsh 0.1.7-rc.2".

That is exactly the failure contract #34 predicted, and its first real
occurrence: the plugin was absent from the profile rather than broken, so every
downstream assertion failed for a reason none of them named.

The 22 pins across seven package.json files now say 0.1.7-rc.2. No source change
was needed -- build, 242 tests + 1 skipped, the web lane, the bundle contract and
ALL REAL-INSTANCE CHECKS pass again, and check:real needed no edit at all.

The diagnosis recipe is recorded in the plan: when a real-instance check fails to
boot, look for "skipping profile bundle" in the log BEFORE reading any assertion.
